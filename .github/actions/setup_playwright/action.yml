name: Setup Playwright
runs:
    using: composite
    steps:
        # Single resolved `playwright` version (see root package.json resolutions).
        # Read from the package the root install scripts use so the cache key matches CI installs.
        - name: Get installed Playwright version
          id: playwright-version
          run: |
              version="$(yarn node -e "console.log(require('playwright/package.json').version)")"
              echo "version=$version" >> "$GITHUB_OUTPUT"
          shell: bash

        # Attempt to restore the correct Playwright browser binaries based on the
        # currently installed version of Playwright (The browser binary versions
        # may change with Playwright versions).
        # Note: Playwright's cache directory is hard coded because that's what it
        # says to do in the docs. There doesn't appear to be a command that prints
        # it out for us.
        - name: Load Playwright browser cache
          uses: actions/cache@v5
          id: playwright-cache
          with:
              path: '~/.cache/ms-playwright'
              key: '${{ runner.os }}-0-playwright-${{ steps.playwright-version.outputs.version }}'
              # As a fallback, if the Playwright version has changed, try use the
              # most recently cached version. There's a good chance that at least one
              # of the browser binary versions haven't been updated, so Playwright can
              # skip installing that in the next step.
              # Note: When falling back to an old cache, `cache-hit` (used below)
              # will be `false`. This allows us to restore the potentially out of
              # date cache, but still let Playwright decide if it needs to download
              # new binaries or not.
              restore-keys: |
                  ${{ runner.os }}-0-playwright-

        # If the Playwright browser binaries weren't able to be restored, we tell
        # playwright to install everything for us.
        - name: Install Playwright browsers
          run: |
              if [ "${{ steps.playwright-cache.outputs.cache-hit }}" = "true" ]; then
                yarn playwright:install
              else
                yarn playwright:install:with-deps
              fi
          shell: bash
