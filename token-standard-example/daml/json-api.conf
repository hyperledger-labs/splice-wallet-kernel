{
  server {
    address = "0.0.0.0"
    port = 7575
    cors-allowed-origins = ["http://localhost:1234"]
  }

  ledger-api {
    address = "localhost"
    port = 6865
  }

  ; TODO check if it does anything
  allow-insecure-tokens = true
}
